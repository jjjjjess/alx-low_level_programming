#include <stdio.h>
/**
 * main -Prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char letter
		for (letter = 'a'; letter <= 'z'; letter++)
			putchar(letter);
			puthcar('\n');
	return (0);
}
